import React from "react";
import { RouteProps } from "react-router-dom";

interface TaskPageProps {
    RouteProps: RouteProps
}

const TaskPage: React.FC<TaskPageProps> = ({ RouteProps }) => {

    return <div>

    </div>
}

export default TaskPage;